crcmod==1.7
pyserial==3.5
colorama==0.4.6
python-dotenv==1.2.1
tqdm==4.67.2
pymavlink==2.4.49
fastapi==0.95.2
uvicorn==0.22.0
pytest==8.3.3
httpx==0.27.2
# MQTT to AWS IoT Core. paho is pure Python (EPL-2.0/EDL-1.0), so it installs
# on the Pi 1 (armv6); awsiotsdk pulls the awscrt C extension, which has no
# 32-bit ARM wheel and would have to be compiled on the board.
paho-mqtt==2.1.0
